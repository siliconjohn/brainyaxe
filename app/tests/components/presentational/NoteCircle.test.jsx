var React = require('react')
var ReactDOM = require('react-dom')
var expect = require('expect')
var TestUtils = require('react-addons-test-utils')

var NoteCircle = require( 'NoteCircle' )

describe( 'NoteCircle', () => {

  it( 'should exist', () => {
    expect( NoteCircle ).toExist()
  })
})
